import { DashboardRouteLoader } from "@/components/dashboard/role/dashboard-route-loader";

export default function AdvisorLoading() {
  return (
    <DashboardRouteLoader
      title="Advisor Workspace"
      subtitle="Loading advisees, appointments, and the operational tools you use to support students."
    />
  );
}

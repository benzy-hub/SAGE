import { DashboardRouteLoader } from "@/components/dashboard/role/dashboard-route-loader";

export default function StudentLoading() {
  return (
    <DashboardRouteLoader
      title="Student Workspace"
      subtitle="Loading your plan, progress, and upcoming support so your dashboard is ready wherever you are."
    />
  );
}
